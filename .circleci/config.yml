version: 2.1
description: Foo

orbs:
    blackfire: blackfireio/php@1.0
    blackfire-agent: blackfireio/agent@dev:1.1

jobs:
    build:
        docker:
            - image: circleci/php:7.3-node

        steps:
            - checkout

            - run:
                  name: Install SOAP
                  command: |
                      sudo apt update && sudo apt install -y libxml2-dev
                      sudo docker-php-ext-configure soap
                      sudo docker-php-ext-install soap

            - blackfire/setup

            # Download and cache dependencies
            - restore_cache:
                  keys:
                      # "composer.lock" can be used if it is committed to the repo
                      - v1-dependencies-{{ checksum "composer.lock" }}
                      # fallback to using the latest cache if no exact match is found
                      - v1-dependencies-

            - run: composer install -n --prefer-dist

            - restore_cache:
                  keys:
                      - node-v1-{{ checksum "package.json" }}
                      - node-v1-
            - run: yarn install
            - save_cache:
                  key: node-v1-{{ checksum "package.json" }}
                  paths:
                      - node_modules

            - run:
                name: Blackfire tests
                environment:
                    APP_DEBUG: 0
                command: php bin/phpunit --group=blackfire

            - blackfire-agent/build-trigger:
                  endpoint: "https://gitlist.demo.blackfire.io/Twig/"

            - save_cache:
                  key: v1-dependencies-{{ checksum "composer.lock" }}
                  paths:
                      - ./vendor
                      - ./bin/.phpunit
