scenario
    name 'The main homepage and blog homepage should answer with a 200 status code'

    visit url('/')
        expect status_code() == 200

    visit url('/blog')
        expect status_code() == 200
